import { UNITS } from "./CONSTANTS";
/**
 * 
 * @param {Number} quantity 
 * @param {String} inUnit 
 * @param {String} outUnit 
 * @returns 
 */
export function convert(quantity, inUnit, outUnit) {
    const convertedQuantity = 0
    return convertedQuantity;
}